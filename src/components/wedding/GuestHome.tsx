import { CalendarDays, CheckCircle, ExternalLink, MapPin } from "lucide-react";
export default function GuestHome({ wedding, phase, guestState, onAction }: any) {
  const date = wedding.wedding_date ? new Date(wedding.wedding_date) : null;
  const days = date ? Math.max(0, Math.ceil((date.getTime() - new Date().setHours(0, 0, 0, 0)) / 86400000)) : null;
  const attending = guestState === "rsvp_confirmed" || guestState === "checked_in";
  return <section className="mx-auto max-w-xl px-5 py-10"><p className="wedding-label">{phase === "wedding_day" || phase === "live" ? "TODAY'S THE DAY" : "YOUR WEDDING COMPANION"}</p><h1 className="mt-3 font-display text-4xl font-light">{wedding.couple_names}</h1><p className="mt-2 font-body text-sm text-muted-foreground">{phase === "wedding_day" || phase === "live" ? `${wedding.ceremony_time || ""} · ${wedding.ceremony_venue || "Venue"}` : days !== null ? `${days} days to go` : "We are glad you are here"}</p><div className="mt-6 rounded-[28px] bg-[#202020] p-5 text-white shadow-xl"><p className="font-body text-xs uppercase tracking-[0.16em] text-white/60">{guestState === "checked_in" ? "Welcome" : attending ? "You're attending" : guestState === "rsvp_declined" ? "We'll miss you" : "Will you be celebrating with us?"}</p>{attending && <p className="mt-2 font-body text-xl">Your place is on the list <CheckCircle className="ml-1 inline h-5 w-5 text-lime-300" /></p>}<div className="mt-5 grid grid-cols-2 gap-2"><button onClick={() => onAction(attending ? "schedule" : "rsvp")} className="rounded-full bg-white px-3 py-3 font-body text-xs font-semibold text-black">{attending ? "View Schedule" : "RSVP"}</button><button onClick={() => onAction(phase === "wedding_day" ? "checkin" : "venue")} className="rounded-full border border-white/30 px-3 py-3 font-body text-xs">{phase === "wedding_day" ? "Check In" : "Get Directions"}</button></div></div><div className="mt-4 grid grid-cols-2 gap-3"><button onClick={() => onAction("schedule")} className="rounded-[22px] bg-white/85 p-4 text-left shadow-sm"><CalendarDays className="h-5 w-5" /><p className="mt-3 font-body text-sm font-semibold">Schedule</p></button><button onClick={() => onAction("venue")} className="rounded-[22px] bg-white/85 p-4 text-left shadow-sm"><MapPin className="h-5 w-5" /><p className="mt-3 font-body text-sm font-semibold">Venue & directions</p></button></div></section>;
}
